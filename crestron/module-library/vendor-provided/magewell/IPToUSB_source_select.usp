#SYMBOL_NAME "IPToUSB_source_select"

#DEFAULT_VOLATILE
#ENABLE_STACK_CHECKING
#ENABLE_TRACE

#USER_SIMPLSHARP_LIBRARY "IPToUSB"

//INPUT
DIGITAL_INPUT source_select_Push;
//type default 'static' which means a record added to device; 'dynamic' means avaliable source in current local network.
//STRING_INPUT input_source_add_type[32];
ANALOG_INPUT source_item_id;

//OUTPUT
STRING_OUTPUT source_select_message;
STRING_OUTPUT source_select_code;
ANALOG_OUTPUT source_select_status;

STRING_INPUT HostIp[16],Cookie[256];
                           
//MainProg is C# class name
MainProg magewell;

PUSH source_select_Push
{
	source_select_status = magewell.source_select(source_item_id);
	source_select_code = magewell.GetCode();
    source_select_message = magewell.GetMessage();
}
CHANGE Cookie
{
    magewell.sSetApiCookie(Cookie);
}
CHANGE HostIp
{
    magewell.SetHostIp(HostIp);
}
Function Main()
{	
    Delay ( 100 );//ms 100

}

